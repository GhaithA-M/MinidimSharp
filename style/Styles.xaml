<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Style for Buttons -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="#FF505050"/> <!-- Dark Gray -->
        <Setter Property="Foreground" Value="#FFFFFF"/> <!-- White -->
        <Setter Property="BorderBrush" Value="#FF707070"/> <!-- Lighter Gray -->
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Style for TextBoxes -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#FF333333"/> <!-- Very Dark Gray -->
        <Setter Property="Foreground" Value="#FFFFFF"/> <!-- White -->
        <Setter Property="BorderBrush" Value="#FF707070"/> <!-- Lighter Gray -->
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

</ResourceDictionary>
